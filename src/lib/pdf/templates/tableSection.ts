// Phase 1 — Table section wrapper. Re-exports the engine primitive so callers
// can think in template terms.
export { placeTable } from "../engine";
export type { PlaceTableOpts } from "../engine";
